const INITIAL_STATE = {
  title: 'Bem vindo ao template de react native da pixter (vendo o reducer)',
};

export default function home(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
