{
  "name": "react-native-template-react-native-template-basic",
  "version": "1.1.9",
  "author": "Pixter",
  "license": "MIT",
  "scripts": {
    "precommit": "lint-staged",
    "commitmsg": "commitlint --edit $HUSKY_GIT_PARAMS"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write --single-quote --no-semi",
      "eslint . --fix",
      "git add"
    ]
  }
}
